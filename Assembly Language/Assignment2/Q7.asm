[org 0x100]

JMP START



START:

	MOV AX,0X0824
	MOV BX,0

	CMP AX,0
	JE QUIT

ITERATION1:
	MOV CX,0
	MOV DX,0
	
ITERATION2:	
	ROR AX,1
	JNC CONDITION
	INC DX
CONDITION:
	INC CX
	CMP CX,16
	JNE ITERATION2
	
	INC BX
	MOV AX,DX
	CMP AX,1
	JNE ITERATION1
	
QUIT:

	
mov ax,0x4c00
int 0x21

;MOV AX,0XC9A3
;	MOV BX,0
;	MOV CX,0
;ITERATION:

;CHECK1:
;	MOV DX,[MSK]
;	AND [MSK],AX
;	
;	JZ CONDITION
;	INC CX
	
;CONDITION:
;	MOV [MSK],DX
;	SHL WORD[MSK],1
;	JNZ CHECK1
	
;	INC BX
;	
;	MOV AX,CX
;	
;	MOV DX,1
;	MOV [MSK],DX
;	
;;	MOV CX,0
	
	;CMP AX,1
;	JNE ITERATION
