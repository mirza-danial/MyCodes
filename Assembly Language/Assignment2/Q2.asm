[org 0x100]
JMP START

MASK1: DW 0X0001
MASK2: DW 0X0002

START:
	MOV AX,0XB6AC
	MOV BX,0
	
ITERATION:	
	MOV SI,[MASK1]
	MOV DI,[MASK2]
	
	AND [MASK1],AX
	AND [MASK2],AX
	
	SHL WORD[MASK1],1
	SHR WORD[MASK2],1
	
	ADD BX,[MASK1]
	ADD BX,[MASK2]
	
	MOV [MASK1],SI
	MOV [MASK2],DI
	
	SHL WORD[MASK1],2
	SHL WORD[MASK2],2
	
	JNZ ITERATION;
	
	MOV AX,BX
mov ax,0x4c00
int 0x21